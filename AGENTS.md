# AGENTS.md

本文件是本项目给 Codex 及其他 AI 开发助手使用的协作规则。开发前必须阅读并遵守本文件；如本文件与 `docs/开发规范.md` 存在冲突，以用户最新要求和本文件中的硬性规则优先。

## 1. 基本协作规则

- 全程使用中文沟通，包括解释、计划、提交说明和变更总结。
- 遵循 KISS 原则，非必要不要过度设计，不提前引入复杂抽象。
- 项目为微信原生小程序，技术栈为 JavaScript + WXML + WXSS + JSON。
- 开发前优先阅读 `docs/开发规范.md`，并按其中的项目结构、数据结构、视觉规范和业务规则执行。
- 本项目以 Codex 为主要 AI 开发助手，同时兼容 Claude Code、Cursor 等工具。
- 默认严格遵守规范；遇到微信小程序平台限制、KISS 原则冲突或明显不合理处，必须先说明原因，再采用更稳妥的实现。

## 2. 微信云开发规则

- 第一阶段即使用微信云开发，不以纯本地存储作为首版方案。
- 食物、抽签记录、用户设置等业务数据默认存储到云数据库。
- 本地存储只用于必要缓存、临时 UI 状态，或云端不可用时的降级说明。
- 云开发环境 ID、数据库集合名、权限配置等实现细节，在具体开发前由用户确认或在任务中明确记录假设。
- 云开发相关逻辑必须统一封装，页面和组件不得直接散落数据库调用。
- 所有云请求必须包含错误处理、空值处理和用户可理解的失败提示。
- 不在组件内部直接读写云数据库；组件只通过 `properties` 接收数据，通过 `triggerEvent` 抛出事件。

## 3. Git 协作规则

- 修改前必须执行 `git status --short`，确认当前工作区状态。
- 不覆盖、不回滚、不格式化用户已有改动，除非用户明确要求。
- 只修改与当前任务相关的文件，避免顺手重构。
- 修改后必须再次执行 `git status --short`，并向用户说明新增或修改的文件。
- 不主动提交、不主动推送；只有用户明确要求“提交”“commit”“推送”或“push”时才执行。
- 提交前必须总结变更范围，并避免把无关文件加入提交。
- Git 提交说明必须使用中文，简洁说明本次变更目的，例如：`初始化小程序基础结构`、`接入云开发数据封装`。

## 4. 代码规范

### 4.1 JavaScript

- 使用 `const` / `let`，禁止使用 `var`。
- 使用 ES6 `import` / `export`，禁止使用 `require`。
- 异步逻辑优先使用 `async/await`，避免回调地狱。
- 字符串默认使用单引号。
- 缩进使用 2 个空格。
- 不使用 `eval()`、`new Function()` 等高风险写法。
- 页面、组件、工具函数中的主要函数都需要写注释，说明用途、关键参数或重要边界。
- 避免无意义逐行注释；复杂业务逻辑、云开发调用、抽签算法、统计逻辑必须有解释性注释。

### 4.2 WXML

- 禁止 inline style，动态计算的位置、旋转角度等少数场景除外。
- `wx:for` 必须配置 `wx:key`。
- 图片必须指定 `mode`，推荐使用 `aspectFit` 或 `widthFix`。
- 列表图片应使用 `lazy-load`。
- class 命名使用 kebab-case 或符合页面语义的 BEM 风格。

### 4.3 WXSS

- 颜色必须优先使用 CSS 变量，禁止随意硬编码颜色值。
- 尺寸单位统一使用 `rpx`，边框 `1px` 等小程序常见场景可例外。
- 间距、圆角、字号优先使用全局变量。
- 避免 `!important`。
- 避免过深嵌套，组件和页面结构保持清晰。

## 5. 项目结构规则

- 主包只放一级 Tab 页面，例如 `pages/home/`、`pages/mine/`。
- 次级页面放入 `packageSub/` 分包，例如编辑食物、全部记录、设置、关于页面。
- 可复用 UI 放入 `components/`。
- 工具函数放入 `utils/`，例如云数据库封装、日期、抽签、统计、常量。
- 默认数据放入 `data/`，例如默认菜单和签文库。
- 静态资源放入 `assets/`，图片资源需控制体积，TabBar 图标使用 PNG。
- 完整项目规范文档放入 `docs/开发规范.md`；如该文件暂不存在，开发前需提醒用户补齐或确认是否从外部文档导入。

## 6. 组件规则

- 组件必须保持单一职责，一个组件只做一类事情。
- 组件通过 `properties` 接收外部数据，不在内部查询存储或云数据库。
- 组件通过 `triggerEvent` 通知父级操作结果。
- 组件内部避免硬编码业务文案；确需默认文案时，应通过属性可配置。
- 组件样式遵守全局颜色、间距、字号、圆角变量。

## 7. 核心业务规则

- 抽签算法必须遵守 `docs/开发规范.md` 中定义的数据结构和流程。
- 抽签记录创建时默认不立即标记 `accepted`。
- 只有用户点击“就吃这个”后，才将对应记录标记为 `accepted: true`。
- 每日抽签次数、最近 N 天排除、无可抽食物降级策略，必须按规范实现。
- 统计逻辑只统计 `accepted: true` 的记录。
- 用户数据必须做空值判断，异常状态必须有可理解的提示。

## 8. 开发顺序

按以下顺序推进，完成一个阶段并验证后再进入下一阶段：

1. 基础框架与微信云开发初始化。
2. 云数据库访问封装与常量定义。
3. 默认数据、日期、抽签、统计等工具函数。
4. 基础组件。
5. 首页抽签流程。
6. 我的页菜单、记录和统计。
7. 分包页面。
8. 动画、音效、适配和性能打磨。

## 9. 自检清单

每次完成代码修改后，至少检查以下内容：

- 是否遵守中文沟通和 KISS 原则。
- 是否只修改当前任务相关文件。
- 是否执行修改前后的 `git status --short`。
- 是否没有覆盖用户已有改动。
- 是否使用微信云开发作为业务数据默认存储方案。
- 是否封装云开发调用，并包含错误处理。
- 是否使用 `const` / `let`，没有 `var`。
- 是否使用 ES6 模块，没有 `require`。
- 主要函数是否有必要注释。
- WXSS 颜色是否使用变量，尺寸是否使用 `rpx`。
- WXML 是否没有不必要的 inline style。
- `wx:for` 是否都有 `wx:key`。
- 图片是否指定 `mode`，列表图片是否使用 `lazy-load`。
- 数据结构是否符合 `docs/开发规范.md`。
- 是否避免无关重构和过度设计。
